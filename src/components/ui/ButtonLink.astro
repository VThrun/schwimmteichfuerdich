---
interface Props {
	href: string;
	variant?: 'primary' | 'secondary' | 'light';
	class?: string;
}

const { href, variant = 'primary', class: className } = Astro.props;
const variants = {
	primary: 'border-pond-800 bg-pond-800 text-white shadow-[0_10px_30px_rgba(16,38,28,0.15)] hover:-translate-y-0.5 hover:border-pond-950 hover:bg-pond-950',
	secondary: 'border-stone-300 bg-white/70 text-stone-800 backdrop-blur hover:-translate-y-0.5 hover:border-pond-600 hover:text-pond-700',
	light: 'border-sand-200 bg-sand-200 text-pond-950 hover:border-white hover:bg-white',
};
---

<a
	href={href}
	class:list={[
		'inline-flex items-center justify-center gap-x-2 rounded-full border px-5 py-3 text-sm font-semibold transition duration-300 focus-visible:ring-3 focus-visible:ring-sand-400 focus-visible:outline-hidden',
		variants[variant],
		className,
	]}
>
	<slot />
</a>
